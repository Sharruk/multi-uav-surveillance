# =============================================================================
# Requirements: Decentralized Multi-UAV Surveillance (STIRS-2025)
# Generated by analysis of: drone_env.py, train.py
# Python: 3.13.x
# Last updated: 2026-05-23
# =============================================================================

# --- CORE SIMULATION (drone_env.py) ---
# Physics engine: 3D rigid-body simulation, raycasting, collision
# NOTE: pybullet_data is bundled INSIDE pybullet — do NOT install separately
pybullet>=3.2.6

# Gymnasium base class (drone_env.py uses DroneSurveillanceEnv(gym.Env))
# Falls back to legacy 'gym' if gymnasium is unavailable
gymnasium>=0.29.0

# Numerical arrays, grid math, raycasting
numpy>=1.26.0

# --- DEEP LEARNING (train.py) ---
# PyTorch: CNN encoder, LSTM, actor/critic heads - CPU only (enforced in code)
# Install CPU-only build to save space on 16GB RAM laptops
torch>=2.1.0

# --- MULTI-AGENT FRAMEWORK (train.py) ---
# PettingZoo: ParallelEnv wrapper for multi-agent RL
pettingzoo>=1.24.0

# --- RL TRAINING (train.py) ---
# Ray RLlib: Distributed MADDPG/PPO training
# ⚠️  WARNING: Ray does NOT support Python 3.13 as of 2026-05.
#    Ray stable supports Python 3.9–3.12 only.
#    For training with Ray RLlib, use Python 3.11 or 3.12.
#    Simulation (drone_env.py) runs fine on Python 3.13.
#    Uncomment the line below only if you have Python ≤ 3.12:
# ray[rllib]>=2.10.0

# --- OPTIONAL / UTILITY ---
# Standard library only — no scipy, matplotlib, pygame, or opencv needed
# by the current codebase. Add below if you extend the project:
# matplotlib>=3.8.0    # for plotting training curves
# scipy>=1.12.0        # for signal processing / filters
# pygame>=2.5.0        # for alternative rendering
# opencv-python>=4.9.0 # for image processing pipelines
